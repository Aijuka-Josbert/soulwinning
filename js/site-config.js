/* ==========================================================
   SITE CONFIG — single source of truth
   Edit this file only. Every page loads it, so one change here
   applies site-wide — no need to touch individual HTML pages.
   ========================================================== */
const SITE_CONFIG = {
  // true  = "Watch Us Live" is clickable and goes to live.html
  // false = "Watch Us Live" is greyed out / not clickable
  liveStreamEnabled: true,
};
